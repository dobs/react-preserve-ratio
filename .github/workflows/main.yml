name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '15'
      - name: Installing dependencies with yarn.
        run: yarn install
      - name: Building with yarn.
        run: yarn build
      - name: Installing examples dependencies with yarn.
        run: |
          cd example
          yarn install
      - name: Building examples with yarn.
        run: |
          cd example
          yarn build
