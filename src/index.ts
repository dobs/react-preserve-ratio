// Mapping to `Props` to avoid the major version bump.
// TODO: Deprecate.
import { PreserveRatioProps } from "./PreserveRatio";
export type Props = PreserveRatioProps;

export { PreserveRatio, PreserveRatioContext, useScale } from "./PreserveRatio";

export { PreserveScale } from "./PreserveScale";
